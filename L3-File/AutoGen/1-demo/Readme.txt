# 安装：请在本项目的虚拟环境中执行下面的命令
pip install -r requiresment.txt
或
pip install -U "autogenstudio"
playwright install

# 运行：打开开发工具的控制台，在本项目的虚拟环境中执行下面的命令运行
autogenstudio ui --port 8080 --appdir ./my-app

# 浏览器打开http://127.0.0.1:8080